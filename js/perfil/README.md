# Estrutura da página de perfil

- main.js: inicialização da página e montagem do fluxo principal.
- api.js: carregamento dos dados do usuário a partir da API.
- utils.js: funções utilitárias, como ocultar e-mail e logout.
- events.js: eventos da interface, como o botão de sair.
